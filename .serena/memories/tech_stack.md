# Tech Stack

- Flutter `3.44.4`, stable channel (framework revision `ad70ec4617`).
- Dart SDK `3.12.2`; pubspec constraint `sdk: ^3.12.2` (Dart 3.12+ required — enables dot-shorthand syntax, see `mem:conventions`).
- Package manager: `pub` (via `flutter pub`). Lockfile `pubspec.lock` present.
- Deps: `cupertino_icons ^1.0.8`. Dev: `flutter_test` (sdk), `flutter_lints ^6.0.0`.
- Targets all 6 platforms (android/ios/linux/macos/web/windows).
- Dev OS: Windows.
